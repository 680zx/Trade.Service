namespace Trade.Service.Settings.CandlestickPatternSettings
{
    internal class HangedManSettings : BaseCandlestickPatternSettings
    {
        public HangedManSettings(IConfiguration configuration)
            : base(configuration, "HangedMan")
        {
        }
    }
}
